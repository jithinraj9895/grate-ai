services:

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqlserver

    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong@Passw0rd" -Q "SELECT 1" -C
      interval: 10s
      timeout: 5s
      retries: 10

    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "YourStrong@Passw0rd"

    ports:
      - "1433:1433"

    volumes:
      - sqlserver_data:/var/opt/mssql

  postgres:
    image: pgvector/pgvector:pg17
    container_name: postgres

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vectordb"]
      interval: 10s
      timeout: 5s
      retries: 5

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vectordb

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  rabbitmq:
    image: rabbitmq:4-management
    container_name: rabbitmq

    ports:
      - "5672:5672"
      - "15672:15672"

    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  api:
    build:
      context: .
      dockerfile: grate/Dockerfile

    container_name: grate-api

    depends_on:
      sqlserver:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy

    ports:
      - "5000:8080"

    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__SqlServerConnection: Server=sqlserver;Database=GrateDb;User Id=sa;Password=YourStrong@Passw0rd;TrustServerCertificate=True
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672

  embedding-worker:
    build:
      context: ./py-embedding-worker
      
    container_name: embedding-worker
    restart: unless-stopped

    depends_on:
      postgres:
        condition: service_started
      rabbitmq:
        condition: service_healthy

    environment:
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: vectordb
      DB_USER: postgres
      DB_PASSWORD: postgres
      MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2

  py-semantic-search:
    build:
      context: ./py-semantic-search
      dockerfile: Dockerfile

    container_name: py-semantic-search

    ports:
      - "8000:8000"

    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: vectordb
      DB_USER: postgres
      DB_PASSWORD: postgres
      MODEL_NAME: sentence-transformers/all-MiniLM-L6-v2

    depends_on:
      postgres:
        condition: service_healthy

volumes:
  sqlserver_data:
  postgres_data:
  rabbitmq_data: